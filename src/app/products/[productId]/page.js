const DynamicProductPage = ({ params, searchParams }) => {
  console.log(params, searchParams);

  return (
    <div>
      <h1>This is DynamicProductPage component</h1>
    </div>
  );
};

export default DynamicProductPage;
